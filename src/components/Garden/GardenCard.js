import React, {Component} from "react"

export default class GardenCard extends Component {
    render() {
        return (
            <React.Fragment>
                "CARD"
            </React.Fragment>
        )
    }
}